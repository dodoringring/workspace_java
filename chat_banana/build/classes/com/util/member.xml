<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">

		<select id="memberList" parameterType="java.util.HashMap" resultType="map">
		select mem_id,mem_name
			from book_member
			where mem_id=#{mem_id}
			and mem_pw=#{mem_pw}
		<where>
			<if test='mem_num > 0'>
				mem_num = #{mem_num}
			</if>
		</where>
	</select>
	<select id="memberList2" parameterType="java.util.HashMap" resultType="com.vo.MemberVO">
		SELECT mem_id, mem_num, mem_name from member
		<where>
			<if test='mem_num > 0'>
				mem_num = #{mem_num}
			</if>
		</where>
		ORDER BY mem_num desc
	</select>
	<select id="Login" parameterType="java.util.HashMap" resultType="map"><!-- map ~ mybatis 예약어 지원 축약가능 -->
		SELECT mem_id, mem_name FROM BOOK_MEMBER WHERE mem_id = #{mem_id} AND mem_pw = #{mem_pw}    
	</select>	
</mapper>
